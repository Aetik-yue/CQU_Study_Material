% 线性规划问题求解
clear;
clc;

% 目标函数系数（最大化转最小化，取负）
f = [-4; -3; -2];

% 不等式约束
A = [2, 3, 1;
     3, 2, 1.5;
     3, 2, 5];
b = [34; 36; 40];

% 变量下界
lb = [0; 0; 0];

% 求解
[x, fval] = linprog(f, A, b, [], [], lb);

% 显示结果
disp('最优解:');
disp(x);
disp('最大利润:');
disp(-fval);
